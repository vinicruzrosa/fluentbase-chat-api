spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/fluencydb}

ai-orchestrator:
  url: ${AI_ORCHESTRATOR_URL:http://localhost:8000}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
